syntax = "proto3";

package todo;

service TodoService {
  rpc GetAllTodos (Empty) returns (GetAllTodosResponse) {}
}

// Empty contract
message Empty {}

message Todo {
  int32 id = 1;
  string text = 2;
  bool completed = 3;
}

message GetAllTodosResponse {
  repeated Todo todos = 1;
}
